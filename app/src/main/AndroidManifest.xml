<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UkulimaIQ"
        android:usesCleartextTraffic="false"
        tools:targetApi="35">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.UkulimaIQ.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.menu.MenuActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.agronomy.AgronomyActivity"
            android:exported="false"
            android:label="@string/title_activity_agronomy"
            android:parentActivityName=".ui.menu.MenuActivity" />

        <activity
            android:name=".ui.climate.ClimateActivity"
            android:exported="false"
            android:label="@string/title_activity_climate"
            android:parentActivityName=".ui.menu.MenuActivity" />

        <activity
            android:name=".ui.prices.PriceActivity"
            android:exported="false"
            android:label="@string/title_activity_price"
            android:parentActivityName=".ui.menu.MenuActivity" />

        <activity
            android:name=".ui.map.MapActivity"
            android:exported="false"
            android:label="@string/title_activity_map"
            android:parentActivityName=".ui.menu.MenuActivity" />
    </application>

</manifest>
